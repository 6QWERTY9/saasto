import { Layoutpage } from "@shared/components/layoutPage"

export const LoginPage = () => {
    return (
        <Layoutpage>
            <div>
                There will be a login form here.
            </div>
        </Layoutpage>
    )
}